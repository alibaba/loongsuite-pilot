export function parseInteractions(events: any[], spans?: any[], options?: { requireOtel?: boolean; contextEvents?: any[] }): Array<{ key: string; records: import('../../../src/types/events.js').AgentActivityEntry[] }>;
